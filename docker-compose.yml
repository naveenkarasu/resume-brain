services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

{
  "name": "resume-brain-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Resume Brain - AI-powered resume analysis mobile app (Android)",
  "scripts": {
    "cap:init": "cap init",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "cap:run:android": "cap run android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  }
}
